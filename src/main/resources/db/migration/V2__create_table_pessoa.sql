CREATE TABLE pessoa (
    id INT PRIMARY KEY AUTO_INCREMENT,
    data_criacao DATE,
    dtype VARCHAR(255),
    cpf VARCHAR(255),
    email VARCHAR(255),
    nome VARCHAR(255),
    senha VARCHAR(255)
);